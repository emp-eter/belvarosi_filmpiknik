The reminder sign-up. Centre it in its own band with a `SectionHeading align="center"` above.

```jsx
<NewsletterForm onSubmit={(email) => save(email)} />
```

Promise exactly what you will send ("egy levél a programról, egy a kezdés előtt") — the brand does not run a general newsletter.
