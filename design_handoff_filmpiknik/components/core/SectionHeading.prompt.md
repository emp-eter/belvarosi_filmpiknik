Every section on the site opens with this, so headings keep one rhythm.

```jsx
<SectionHeading eyebrow="Négy helyszín" title="Négy helyszín, egy séta" lead="Minden helyszín pár perc járásra van egymástól." />
<SectionHeading title="A műsor" actions={<DayTabs days={days} value={0} onChange={setDay} />} />
```

Use `align="center"` only in the newsletter band. The `h2` is always Space Grotesk — Kaushan Script is reserved for the festival name.
