Sits in the hero, under the date line. One per page — a second countdown reads as a sales tactic, which is off-brand for a free festival.

```jsx
<Countdown target={new Date(2026, 8, 16, 18, 0, 0)} />
```

It clamps at zero rather than counting up.
