Stack these in a `display:flex; gap:2px` column over a gold background so the 2px gaps read as gold hairlines.

```jsx
<VenueRow number={1} name="Dobó tér" when="19.00 térzene · 20.00 film" description="A fesztivál főtere." />
```

Numbering always starts at 1 at the main square and follows the walking order; keep it in sync with the map pins.
